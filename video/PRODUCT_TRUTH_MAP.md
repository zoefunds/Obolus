# PRODUCT_TRUTH_MAP.md — Obolus

| Claim / feature | Status | Evidence | Safe wording | Visual treatment |
|---|---|---|---|---|
| Inactivity timers / trusted executors are the two existing mechanisms, both broken | working (framing claim) | README §1 | Use almost verbatim — it's the project's own stated thesis | Cold open: two broken mechanisms shown as failing patterns, not narrated abstractly |
| Vault = locked GEN + named beneficiary + named subject | working | README §2, screenshot: New Vault form | State directly | Reconstructed form UI, real field labels (Beneficiary Address, Subject Name, Birth Year, Known Aliases, Contest Window, GEN to Escrow) |
| Anyone can submit a death claim + bond, opening a contest window | working | README §2, contract `submit_death_claim` L665-740 | State directly | Vault detail screenshot (CLAIM_PENDING, countdown "Ends in 23H 55M 41S") |
| Anyone can contest with counter-evidence + bond before window closes | working | README §2, contract `contest_claim` L740-803 | State directly | "Submit Counter-Evidence" button visible in screenshot |
| Validators independently fetch every URL and reach a verdict; must agree exactly on category + confidence band | working | contract L803-1016 (`gl.nondet.web.render/get`, `gl.eq_principle.prompt_comparative`) | State directly — this is GenLayer's actual consensus primitive, name it | Architecture/mechanism scene: URLs → fetch → per-validator reasoning → consensus check → verdict |
| Three-way verdict: CONFIRMED / REFUTED / INCONCLUSIVE, confidence bands LOW/MED/HIGH, only HIGH moves state | working | contract L1019-1139 | State directly | State-machine animation: SUBMITTED → EVIDENCE FETCHED → VALIDATED → one of three terminal states |
| Bond routing per outcome (return/forfeit rules) | working | README table in §4, contract L1083-1139 | State directly, briefly (do not over-explain in voiceover; can live in on-screen text/diagram only) | Small table/diagram, not full narration |
| An actual claim has resolved CONFIRMED or REFUTED in production | **not evidenced** — dashboard shows CONFIRMED: 0, REFUTED: 0 | Dashboard/Admin screenshots | Never say "has already paid out a beneficiary." Say "the mechanism is live and tested; no claim has yet completed a full resolution in this environment." | Do not animate a fake payout. Show the CLAIM_PENDING / contest-window state only, which is real |
| All payouts are pull-based (withdraw, not push) | working | README §4 | State directly | One line of on-screen text over the Admin/balance context, not a fabricated withdraw animation |
| Writes signed by the user's own connected wallet, not a server key | working | README "How a write actually happens", `genlayerBrowser.js` | State directly | Simple diagram: wallet icon → signs → contract, no server key in the path |
| Admin cannot move funds, cannot block resolve/withdraw even while paused | working | README §5 roles table, Admin screenshot copy ("Never blocks resolve_claim or withdraw") | State directly — strong trust point | Admin screenshot close-up on that exact sentence |
| Platform is live on GenLayer StudioNet with a real deployed contract address | working | README header, live URLs | State directly, but qualify StudioNet as a testnet, not mainnet | Contract address shown in monospace, on-chain address styling |
| 58+4 passing contract tests | working | README §8, repo `tests/direct/` | State directly as evidence of rigor | Brief on-screen stat, not dwelt on |
| Built for/on GenLayer, an L1 with LLM-judged Intelligent Contracts | working | README intro, genlayer.com | State directly; this is the ecosystem relationship, explain via contract behavior not logo | Use the actual mechanism (fetch → LLM reasoning → consensus) as the explanation, GenLayer branding as a small attribution only |
| Wallet network auto-switch to StudioNet on connect | working | README "How a write actually happens" | May mention briefly if time allows | Optional, cut if tight on runtime |
| Backend relayer write routes exist | working, but explicitly a fallback not primary path | README "Known limitations" | Do not feature — not part of the primary trust story; the film should emphasize wallet-signed writes only | Omit from film entirely to avoid diluting the "your wallet signs it" trust point |
| Any roadmap/future integration beyond what's stated | **unclear** — no roadmap document found | n/a | Do not invent one. If a "vision" line is needed for the close, keep it a values statement, not a feature promise | Closing statement is philosophical, not a feature list |
| Obolus prevents fraud / guarantees correct outcomes | **unclear / must not overclaim** | INCONCLUSIVE is an explicit abstention path, not a guarantee of catching every case | Say "gives evidence a real trial, not a guarantee against every failure mode" | Do not show a "100% accurate" or similar stat anywhere |
